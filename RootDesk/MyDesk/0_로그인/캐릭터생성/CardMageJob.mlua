@Component
script CardMageJob extends Component

	@EventSender("Self")
	handler HandleButtonClickEvent(ButtonClickEvent event)
		local uiRoot = _EntityService:GetEntityByPath("/ui/0_캐릭터생성-UI")
		if isvalid(uiRoot) then
			uiRoot.CharacterCreateManager:SelectJob("mage")
		end
	end

end